name: CI

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

permissions:
  contents: read
  checks: write

jobs:
  build:
    name: Build & Verify
    uses: ./.github/workflows/build.yml
